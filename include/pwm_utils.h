/*
#ifndef PWM_UTILS_H
#define PWM_UTILS_H

#include "driver/ledc.h"

void pwm_init(int pin, int channel, int freq, int duty);
void pwm_set_duty(int channel, int duty);
void pwm_start(int channel);

#endif // PWM_UTILS_H
*/